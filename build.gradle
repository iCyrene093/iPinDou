// The app module conditionally applies the Android Gradle Plugin when an
// installed Android SDK platform and a reachable/cached AGP are available.
// Otherwise :app:assembleDebug and :app:assembleRelease fall back to the repository's offline APK builder.
